# .env.example
#
# PURPOSE:
#   Documents every environment variable that server.js requires.
#   This file IS committed to the repository — it serves as documentation.
#
#   The actual .env file (with real values) is NEVER committed to the
#   repository. Add ".env" to .gitignore to enforce this:
#     echo ".env" >> .gitignore
#
# SETUP INSTRUCTIONS:
#   1. Copy this file and name the copy ".env":
#        cp .env.example .env
#   2. Fill in the real values in .env.
#   3. server.js reads .env automatically via "import 'dotenv/config'".
#
# PRODUCTION (Render / Railway):
#   Set these same variables in the hosting platform's environment variable
#   dashboard. The .env file is only used for local development.

# Full PostgreSQL connection URI for TimescaleDB Cloud.
# Format: postgres://USER:PASSWORD@HOST:PORT/DBNAME?sslmode=require
# Replace <YOUR_PASSWORD> with the password from the TimescaleDB Cloud console.
DATABASE_URL=postgres://tsdbadmin:<YOUR_PASSWORD>@gdj8h4wj0e.kfltst1kf3.tsdb.cloud.timescale.com:37859/tsdb?sslmode=require

# The GitHub Pages URL where index.html is hosted.
# server.js passes this to the cors() middleware so the browser permits
# fetch() responses from the backend to be read by script.js.
# Format: https://yourusername.github.io  (no trailing slash)
# If the repository is in a subdirectory: https://yourusername.github.io/repo-name
ALLOWED_ORIGIN=https://yourusername.github.io

# The port the Express server listens on.
# Hosting platforms like Render set this automatically via their own PORT variable.
# For local development, 3000 is the conventional default.
# server.js uses: const PORT = process.env.PORT || 3000
# So this line is optional locally — the fallback handles it.
PORT=3000
